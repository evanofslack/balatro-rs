Drop real, private `.jkr` files here (exported `meta.jkr`, `profile.jkr`, `save.jkr`
from an actual Balatro install) to exercise the real-fixture tests in `src/profile.rs`
and `src/save_snapshot.rs` against real data.
